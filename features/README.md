# Features

Eleven at launch, with a folder when a feature has scripts. Frontmatter: `name`, `description` and `category` (`planning`, `quality` or `continuity`), read by the installer to group the content step.
