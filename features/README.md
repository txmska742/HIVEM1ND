# Features

Eleven in the base, plus the ones a knowledge module ships, with a folder when a feature has scripts. Frontmatter: `name`, `description` and `category` (`planning`, `quality` or `continuity`), read by the installer to group the content step.
